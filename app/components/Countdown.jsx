var React = require('react');

var Countdown = React.createClass({
  render: function () {
    return(
      <div>
        <h1 className="text-center page-title">Countdown</h1>
       <p>This is Countdown page</p>
      </div>

    );
  }
});

module.exports = Countdown;
